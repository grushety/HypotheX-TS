# Tickets have moved

Active tickets are in **`.claude/tickets/`** (Claude Code convention).

Legacy tickets from the OpenAI Codex era are archived at `archive/tickets-mvp-legacy/`.

Do not place new ticket files at this path.
