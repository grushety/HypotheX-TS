# Model Skeleton

This top-level directory is reserved for future model-specific code that consumes the shared contracts in `schemas/`.

Expected future contents:

- segmentation proposal modules
- prototype or encoder components
- ontology-aware scoring helpers
- adapters that emit payloads matching the shared schema layer
