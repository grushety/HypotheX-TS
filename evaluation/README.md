# Evaluation Skeleton

This top-level directory is reserved for future technical and study evaluation code that consumes exported session logs and benchmark fixtures.

Expected future contents:

- metric pipelines
- baseline comparison scripts
- dataset split manifests for experiments
- analysis helpers that read the shared contracts in `schemas/`
